import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    labelComponent: {
        flexDirection: 'row',
        paddingLeft: 20
    },
    normal: {
        color: '#000000'
    },
    bold: {
        fontWeight: 'bold'
    },
    lightness: {
        color: '#cccccc'
    },
    description: {

    }
});

export default styles;
